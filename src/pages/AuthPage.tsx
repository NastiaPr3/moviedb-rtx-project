import {Authentication} from "../components/AuthContainer/Authentication";

const AuthPage = () => {
    return (
        <div>
            <Authentication/>
        </div>
    );
};

export {AuthPage};